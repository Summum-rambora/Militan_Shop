/*
package models;

import com.example.militanshop.models.Feedback;
import com.example.militanshop.models.Product;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class FeedbackTest {

    @Test
    public void testFeedbackConstructorAndGetters() {
        Product product = new Product("Test Product", "Description", 10.0);
        Feedback feedback = new Feedback("Good product", "/path/to/file", product);

        assertEquals("Good product", feedback.getText());
        assertEquals("/path/to/file", feedback.getFilePath());
        assertEquals(product, feedback.getProduct());
    }

    @Test
    public void testFeedbackSetters() {
        Feedback feedback = new Feedback();
        feedback.setText("New feedback");
        feedback.setFilePath("/new/path/to/file");

        assertEquals("New feedback", feedback.getText());
        assertEquals("/new/path/to/file", feedback.getFilePath());
    }
}

*/