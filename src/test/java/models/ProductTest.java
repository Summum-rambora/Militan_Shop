package models;

/*
import com.example.militanshop.models.Product;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class ProductTest {

    @Test
    public void testProductConstructorAndGetters() {
        Product product = new Product("Product Name", "Product Description", 99.99);

        assertEquals("Product Name", product.getName());
        assertEquals("Product Description", product.getDescription());
        assertEquals(99.99, product.getPrice());
    }

    @Test
    public void testProductSetters() {
        Product product = new Product();
        product.setName("New Name");
        product.setDescription("New Description");
        product.setPrice(49.99);

        assertEquals("New Name", product.getName());
        assertEquals("New Description", product.getDescription());
        assertEquals(49.99, product.getPrice());
    }
}

*/